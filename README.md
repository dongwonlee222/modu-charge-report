# modu-charge-report

모두의충전 전용 리포트 저장소 분리본입니다.

## 포함 파일

- `index.html`로 사용할 수 있는 `modu-charge.html`
- `giftcard.html`
- `giftcard/index.html`
- `modu_charge_*` 리포트/데이터 파일
- `kakao-giftcard-*` 리포트 파일

## 다음 단계

1. 이 폴더에서 `git init`
2. 새 GitHub repo 생성
3. `main` 브랜치로 push
4. GitHub Pages를 `/root` 기준으로 활성화
